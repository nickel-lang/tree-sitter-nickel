{
  f = fun x => x,
  #     ^ keyword.function
  #         ^ parameter

  g = fun x y => x + y,
  #         ^ parameter
  #           ^ parameter

  result = %is_number% 42,
  #          ^ function.builtin
}
